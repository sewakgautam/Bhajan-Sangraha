name: bhajan_sangraha
description: "Nepali/Hindi devotional bhajan songbook with offline lyrics, bookmarks, and daily Gita quotes."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+11

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  connectivity_plus: ^6.0.5
  google_fonts: ^6.2.1
  firebase_auth: ^5.3.1
  firebase_messaging: ^15.1.3
  google_sign_in: ^6.2.1
  share_plus: ^10.1.2
  flutter_native_splash: ^2.4.8
  flutter_local_notifications: ^22.2.0
  google_mobile_ads: ^9.0.0
  youtube_player_iframe: ^6.0.2
  firebase_crashlytics: ^4.3.10
  package_info_plus: ^9.0.1
  url_launcher: ^6.3.2
  wakelock_plus: ^1.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: true
  # Used only as the legacy (pre-Android 8, non-adaptive) fallback icon —
  # those aren't masked by the OS, so the full-bleed circle badge is fine
  # as-is here without safe-zone padding.
  image_path: "assets/branding/logo Complete round.png"
  min_sdk_android: 21
  # Matches the badge's own maroon fill (#350712, sampled from the source
  # art) instead of white — the foreground below has transparent (not
  # colored) safe-zone padding, so whichever mask shape a launcher applies
  # (circle/squircle/rounded square), the background shows through the
  # margin as the same maroon, reading as one seamless badge either way.
  adaptive_icon_background: "#350712"
  adaptive_icon_foreground: "assets/branding/splash_icon_android12.png"

# Classic (pre-Android 12): background_image (not image) so splash.jpg is
# stretched to actually fill the screen — `image` alone centers at native
# pixel size with no scaling, which is what made it look huge/cropped.
# Android 12+: the system splash API only allows a small centered icon on a
# color (no arbitrary fullscreen image); icon_background_color/color match
# the badge's own maroon so there's no visible padding of any color, same
# reasoning as adaptive_icon_background above.
flutter_native_splash:
  background_image: assets/splash.jpg
  fullscreen: true
  android_12:
    image: assets/branding/splash_icon_android12.png
    icon_background_color: "#350712"
    color: "#350712"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/festivals.json
    - assets/branding/logo.png
    - assets/branding/splash_icon_android12.png
    - assets/splash.jpg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
